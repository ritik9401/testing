_format_version: "3.0"
add-plugins:
- selectors:
    plugins:
    - config:
        origins:
        - "*"
      enabled: true
      name: cors
      protocols:
      - grpc
      - grpcs
      - http
      - https
